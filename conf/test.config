params {
  // Path to test input data
  input                      = "${projectDir}/data/Example-local.csv"
}

// Max resources for the test data can be reduced
process {
  resourceLimits = [ cpus: 2, memory: 6.GB, time: 6.h ]
}
